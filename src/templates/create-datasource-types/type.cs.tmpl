// schema-version: {{schemaVersion}}
using Deterministic.Types;

namespace Backend.Types.Datasource;

{{#simpleDoc}}/** Type {{className}}. */
{{/simpleDoc}}{{#descriptionDoc}}/**
 * Type {{className}}.
 * Datasource type: {{datasourceType}}.
 * Target: StandardCrud.
 * Fields: {{fieldCount}}.
 */
{{/descriptionDoc}}public class {{className}} : {{base}}<{{typeArgs}}>
{
{{#fields}}
    public {{csType}} {{ident}} { get; set; }
{{/fields}}
}
